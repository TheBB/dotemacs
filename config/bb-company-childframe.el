(bb-package company-childframe
  :init
  ;; I'd like to use company-childframe at some point
  ;; but right now it's way too slow
  ;; (with-eval-after-load 'company
  ;;   (company-childframe-mode))
  )
